name: C/C++ CI

on:
  push:
    branches: [ "master","anarg" ]
  pull_request:
    branches: [ "master","anarg" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - name: make test
      run: make test
